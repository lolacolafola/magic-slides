# Generated test renders (never committed)
test-output/

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Artist-specific YAMLs stay local — never commit real client/pitch content.
# Only example-artist.yaml is whitelisted as the public reference.
examples/*.yaml
!examples/example-artist.yaml

# Working notes contain detailed deck-derivation context (specific artist analysis).
# Keep local; not portfolio-useful, IP-sensitive.
notes/
